# Preset aliases start
alias ls='ls --color'
alias ll='ls -alhF'
alias grep='grep --color'
alias path-info='echo $PATH | tr : \\n'
alias fpath-info='echo $FPATH | tr : \\n'
alias reload-shell='exec $SHELL -l'
alias tree='tree -C'
# Preset aliases end
